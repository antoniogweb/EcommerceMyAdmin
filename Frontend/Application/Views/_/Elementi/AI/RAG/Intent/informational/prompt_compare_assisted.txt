Return ONLY JSON:
{
"intro_text": "string",
"items": [
{
"title": "string",
"comment": "string",
"in_depth": [
{
"text": "string",
"url": "string",
"comment": "string"
}
]
}
]
}

Rules:

* Reply in the following language: "[LINGUA]".
* Use ONLY context_items as the sole source of truth.
* General technical knowledge may be used ONLY according to the Grounding rules.
* Identify all context_items relevant to the user's comparison.
* Combine ALL relevant context_items into ONE single synthesized item.
* The output MUST NOT preserve the one-context-item-to-one-output-item structure.
* The "items" array MUST contain exactly one element whenever at least one relevant context_item exists.
* The single output item must integrate information from every relevant context_item.
* Do not create separate items for separate products, sources, documents, or context_items.
* Ignore only context_items that are completely unrelated to the user's question.
* Do not include an "id" field, because the output item is generated from multiple context_items.
* No extra text outside the JSON.

Comparison synthesis:

* Treat all relevant context_items as parts of one combined information set.
* Produce one unified comparison using the sum of the relevant information.
* The title must be generated from the compared subjects found across context_items.
* The comment must be generated by combining, reorganizing, and comparing the relevant information found across context_items.
* The in_depth array must combine all relevant links associated with the compared subjects.
* Do not summarize each context_item separately.
* Do not create one paragraph mechanically corresponding to each context_item.
* Organize the final answer around the products, characteristics, similarities, differences, uses, and limitations relevant to the user's question.
* When multiple context_items describe the same subject, merge their information and avoid repetition.
* When different context_items describe different compared subjects, integrate them into the same comparison.
* When a detail exists for only one subject, state only what is supported for that subject.
* Do not infer that a missing detail represents a difference.
* Do not declare one product better unless context_items explicitly support that conclusion.
* General technical knowledge may only help interpret the compared products.

Grounding rules:
* context_items are the primary and mandatory source of information.
* If no context_items are relevant, return:
  intro_text="Non ho trovato alcuna informazione pertinente."
  items=[]
* Never answer using general knowledge if no relevant context_items are available.
* General technical knowledge may only be used to complement the retrieved context_items.
* General technical knowledge may only help interpret the retrieved products or categories.
* It must never replace missing product documentation.
* When using general technical knowledge, clearly distinguish it from confirmed product information using cautious expressions such as:
  "in general",
  "typically",
  "usually",
  "it may",
  "it should",
  or equivalent expressions in the user's language.
* Never invent product-specific specifications, values, certifications, compatibility, resistance levels, guarantees, or other technical data that are not explicitly present in context_items.
* Never present general technical knowledge as a confirmed property of a specific product.
* If at least one context_item is relevant to the user's comparison, produce the synthesized comparison. If the available information supports only a partial comparison, clearly explain the limitation instead of treating it as "no match". Return an empty items array only when none of the context_items is relevant.

If no match:

{
"intro_text": "Non ho trovato alcuna informazione pertinente.",
"items": []
}

title:

* Create one concise title for the complete comparison.
* The title must represent all compared subjects, not only one context_item.
* Preserve exact product and brand names when available.
* Do not copy the title of only one source.
* MUST be no longer than 15 words.

comment:

* MUST be no longer than 100 words.
* Write one unified, direct and informative comparison.
* The comment should primarily rely on context_items.
* You may use general technical knowledge ONLY to complement the retrieved context_items.
* Use information from ALL relevant context_items.
* Preserve product and brand names exactly as written in context_items.
* Highlight product names and brand names using **bold**.
* Do not mention context_items, sources, documents, retrieval, articles, or supplied text.
* Avoid phrases such as "il testo spiega", "l'articolo dice", "la fonte indica" or "dalle informazioni fornite".
* Do not repeat the same information.
* Clearly distinguish supported similarities and differences.
* Clearly state when the available information does not allow a complete comparison.

Links handling:

* A LINKS_JSON section may be provided containing objects with "text" and "url".
* Build one single in_depth array using links relevant to any of the subjects present across all relevant context_items.
* Add links only for products, brands, categories, or items explicitly mentioned in context_items and relevant to the comparison.
* Prefer exact product-name matches over generic category matches.
* Use the original link text and URL exactly as provided in LINKS_JSON.
* Do not invent, modify, shorten, or rewrite links.
* Remove duplicate URLs.

in_depth structure:

{
"text": "exact link text",
"url": "exact url",
"comment": "short explanation"
}

in_depth comment rules:

* MUST be between 5 and 20 words.
* Describe directly what is linked and why it is relevant.
* Do not repeat the exact link text.
* Do not mention sources, context_items, documents, or LINKS_JSON.

in_depth rules:

* Include all clearly relevant links from the combined context.
* Include up to 10 links in total.
* If no relevant links exist, return an empty array.

intro_text:

* MUST be no longer than 12 words.
* Introduce the unified comparison directly.
