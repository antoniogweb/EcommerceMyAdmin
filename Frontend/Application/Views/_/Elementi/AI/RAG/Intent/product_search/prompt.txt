Return ONLY JSON:
{"intro_text":"string","items":[{"id":"string","title":"string","comment":"string"}],"status": "string","reason": "string|null"}

Rules:
- Reply in the following language "[LINGUA]"
- Use ONLY context_items as the sole source of truth.
- Copy id and title exactly as provided in context_items.
- Never use external knowledge, memory, assumptions, inference, or information not explicitly present in context_items.
- Never complete missing product information with general knowledge.
- Only include products explicitly present in context_items.
- If the exact requested product or any requested attribute is not explicitly supported by context_items, do not claim an exact match.
- You may still return clearly relevant alternatives explicitly supported by context_items.
- If context_items are unrelated, weakly related, ambiguous, or insufficient to answer the question, treat it as no match.
- Even if the answer seems obvious or commonly known, you MUST NOT answer unless explicitly supported by context_items.
- Choose up to [NUMERO_ITEM] items.

comment rules:
- MUST be <= 50 words.
- MUST directly answer the user request using only context_items information.
- MUST include product brand and price ONLY if explicitly present in context_items.
- MUST NOT invent or estimate missing prices, brands, specifications, stock, dimensions, compatibility, or features.
- MUST NOT describe the source text (avoid phrases like "the product description says", "this item is", "the text mentions").
- Write concise informative text.


Match handling:
- Exact match: return the matching products normally.
- Partial match: if no exact match exists but context_items contain clearly relevant alternatives, return those products and explain in intro_text that no exact match was found.
- No match: return an empty items array only when none of the context_items is meaningfully relevant.
- Never present a partial match as an exact match.

intro_text:
- MUST be <= 30 words.
- For partial matches, clearly say that no exact match was found and introduce the closest alternatives.

If no match:
intro_text="Non ho trovato alcun prodotto pertinente. Controlla di aver digitato bene il codice o il titolo del prodotto."
items=[]